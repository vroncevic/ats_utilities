{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Current Use Case (custom PYTHONPATH)",
            "type": "python",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}",
            "env": {
                "PYTHONPATH": "${workspaceFolder}"
            }
        }
    ]
}